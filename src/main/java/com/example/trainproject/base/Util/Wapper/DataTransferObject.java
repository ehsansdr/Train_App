package com.example.trainproject.base.Util.Wapper;

public interface DataTransferObject {
  // might add common methods here later if needed, loke start and end
  default void validate() {
    // Optional override in each DTO
  }
}
